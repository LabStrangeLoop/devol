population_size: 256
num_steps: 50
param_dim: 10
sigma_m: 0.8
seed: 42

schedule:
  type: cosine
  epsilon: 0.0001

fitness:
  mapping: direct
  temperature: 1.0
  normalize: true
  shift_negative: true

distance:
  type: euclidean
  latent_dim: 2
